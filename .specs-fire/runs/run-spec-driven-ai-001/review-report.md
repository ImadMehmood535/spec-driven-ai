---
run: run-spec-driven-ai-001
work_item: project-scaffold
intent: developer-user-module
generated: 2026-09-12T13:22:00Z
reviewer: fire-builder (code-review skill)
---

# Code Review: Backend Project Scaffold

Reviewed all 40 files created in this run against
`.specs-fire/standards/coding-standards.md` and `testing-standards.md`.

## Findings Fixed

| # | Finding | Severity | Action |
|---|---------|----------|--------|
| 1 | `forRoutes('*')` is a known throw under Express 5, which `@nestjs/platform-express` 11 uses. Nothing in the run exercised the middleware chain, so a first-startup crash was plausible. | **high** | Added `AppController.spec.ts`, which boots a real Nest app and proves the chain registers and runs. The pattern works on this version; the risk is now covered by a test rather than assumed. |
| 2 | `ApiRoute.App = ''` was dead — `AppController` uses a bare `@Controller()`. | low | Removed. `SwaggerTag.App` is genuinely used and stayed. |
| 3 | 15 lint errors and 3 warnings in the new spec files, from `any`-typed fakes tripping `no-unsafe-member-access`. | medium | Retyped the fakes with real interfaces (`AuditableInstance`, `BulkUpdateOptions`, typed handler signatures) and cast `getHttpServer()` to `Server`. Lint is clean with **no rule relaxations**. |
| 4 | Coverage was 61.03%, below the 80% standard, with all wiring untested. | medium | The HTTP test in finding 1 lifted it to 83.11%. |

All four were re-verified: 96 tests pass, lint clean, build succeeds.

## Observations — no action taken

**`esModuleInterop` is absent**, inherited from the reference service's tsconfig. Consequence
for later work items: CommonJS packages need `import * as x from 'x'`, not `import x from 'x'`.
It cost one failed test run here. Left as-is to stay consistent with the reference, but worth
knowing before it bites again.

**`DatabaseModule` has `models: []` with `autoLoadModels: true`.** Correct while no models
exist. `database-migrations` and the entity slices must register their models or rely on
autoload; the empty array is not a bug but it is a place to look if a model ever seems missing.

**`RequestLoggerMiddleware` branch coverage is 82.6%.** The uncovered branches are the colour
lookups for HTTP verbs and status ranges the tests do not produce. Cosmetic; not worth tests.

**`Guards.ts` branch coverage is 96.42%**, line 29 — the `!Number.isInteger` half of
`optionalId`'s guard when the value is a non-integer. Covered for `0`, `-2`, `2.5`; the
remaining path is unreachable in practice.

## Standards Compliance

| Check | Result |
|-------|--------|
| Layer direction `api → application → domain`, infrastructure implements ports | Followed |
| Controllers hold no business logic | `AppController` delegates to `AppService` |
| No `*Model` imported outside `infrastructure`/`domain` | Confirmed |
| Path aliases used across layers; relative imports only within a folder | Confirmed |
| One exported class per file, PascalCase filenames | Confirmed |
| Errors thrown from `shared/errors`, translated once in the filter | Confirmed; no `HttpException` outside `api` |
| Swagger decorators on routes | `AppController` documented |
| Prettier settings (`singleQuote`, `trailingComma: all`) | Enforced via ESLint, clean |
| `synchronize: false` (NFR-7) | Set, with a comment explaining why |
| No credential in a response, log, or fixture (NFR-3) | Enforced by `Redaction` and proven by HTTP test; fixtures use obvious placeholders |
| Comment density matches the reference (near-zero, only non-obvious notes) | Followed — comments only on `Redaction`, the `synchronize` line, and the HTTP test's scope |

## Deviations From the Reference Service

Both deliberate, both recorded in the plan:

1. **`EntityStatus` omits `DRAFT`.** It has no meaning for access control, and a test asserts
   it is rejected.
2. **Request bodies are redacted before logging.** The reference logs bodies wholesale, which
   would print plaintext passwords once login exists. Redaction landed before any credential
   route could exist.

## Verdict

**Ready for completion.** No open findings. Two low-severity items in the test report
(live database connection and Swagger rendering) are blocked on the Docker daemon and do not
affect the code.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-spec-driven-ai-001*
