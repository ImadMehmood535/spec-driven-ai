---
run: run-spec-driven-ai-008
work_item: authentication-login
intent: developer-user-module
generated: 2026-09-12T19:40:00Z
status: passing
---

# Test Report: Credential Verification and JWT Login

## Summary

| Category | Passed | Failed |
|----------|--------|--------|
| Unit (login handler) | 14 | 0 |
| Unit (JWT config) | 7 | 0 |
| Live API (real database) | 12 checks | 0 |
| **Total** | **339 tests + 12 live checks** | **0** |

## Acceptance Criteria Validation

- ✅ **Login returns a signed JWT (FR-U6)** — live, with both email and username as identifier
- ✅ **Password verified against the stored bcrypt hash** — live
- ✅ **Wrong password → 401** — live
- ✅ **Unknown user → 401, indistinguishable from a wrong password** — live, byte-identical body
- ✅ **Deactivated user cannot authenticate (FR-U7)** — live: correct password, still 401,
  with no hint the account exists
- ✅ **Claims carry sub / username / role / permissions (D-2)** — live, decoded below
- ✅ **Permission claims come from the shared resolution** — so every FR-AC3 exclusion applies
  to the token; a unit test asserts an empty resolution yields empty claims
- ✅ **Secret and lifetime from configuration (NFR-8)** — 7 tests
- ✅ **App fails fast without `JWT_SECRET`** — live, verified below
- ✅ **Login body redacted (NFR-3)** — live: 0 plaintext occurrences, 10 `[REDACTED]`
- ✅ **No refresh token, logout, revocation, or password-reset route (D-2)** — only `/auth/login`
- ✅ **Swagger documents the route and its failure (NFR-2)**

## Live Verification

**Token claims**, decoded from a real login:

```json
{
  "sub": "282271b5-882c-49ff-a51f-7a1ecc16256f",
  "username": "ahmed",
  "role": "Developer Admin",
  "permissions": ["project.create"],
  "iat": 1789231280,
  "exp": 1789234880          // exactly 1h, from JWT_EXPIRES_IN
}
```

`contains $2b$ (a hash): false` · `contains the plaintext: false`

**Uniform failure** — the requirement that prevents user enumeration:

```
wrong password            → 401 {"message":"Invalid credentials.","error":"UnauthorizedError"}
unknown user              → 401 {"message":"Invalid credentials.","error":"UnauthorizedError"}
deactivated + correct pw  → 401 {"message":"Invalid credentials.","error":"UnauthorizedError"}

byte-for-byte comparison of all three bodies: IDENTICAL
```

**Timing parity** — the other half of enumeration resistance:

```
existing user, wrong password  → 0.20s
unknown user                   → 0.19s
```

Indistinguishable. Without the dummy-hash verify on the absent path, the unknown-user case would
return in a few milliseconds against ~200ms for a real bcrypt comparison at cost 12.

**Fail-fast on a missing secret:**

```
$ env -u JWT_SECRET node dist/main
JWT_SECRET is not set. The application will not start without it —
a default signing key would make every token forgeable.
```

**Redaction:** 0 occurrences of any plaintext password in the app log, 10 of `[REDACTED]`, and 0
occurrences of the token (response bodies are not logged).

## Issues Found

| Issue | Severity | Status |
|-------|----------|--------|
| I installed `@nestjs/jwt@12` while the rest of the stack is Nest **11** — a version mismatch that surfaced as an ESM loading failure in Jest | medium | **fixed** — pinned to `^11`. Worth noting the compiled app *did* run, because Node 24 can `require()` ESM natively; only Jest exposed it. Taking `latest` was the mistake |
| `AuthModule` did not declare `ConfigModule`, relying on the root module making it global | medium | **fixed** — declared explicitly. It worked in production and failed the moment a test composed the module in isolation, which is exactly the drift a module should not have |
| `expiresIn` typed as a template literal in v11, not `string` | low | **fixed** — narrowed via `SignOptions['expiresIn']` with a comment, rather than casting to `any` |
| `entityStatus` compared across a string/enum boundary | low | **fixed** — typed `AuthUserReadModel.entityStatus` as `EntityStatus`, which the native DB enum guarantees |
| 3 lint errors from `mock.calls[0][0]` | low | **fixed** — calls array typed before indexing |

## Ready for Completion

- [x] All tests passing — 339
- [x] Every acceptance criterion validated, 12 against a live database
- [x] No critical issues open

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-spec-driven-ai-008*
